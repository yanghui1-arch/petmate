# Requirements
Node.js 22.14.0 (必须为此版本，否则无法跑起)

# Start
## 启动项目
### Windows
在windows上直接在终端运行下面的脚本即可，遇到warning是正常情况，运行好后尝试跑开发环境命令。
```powershell
.\scripts\windows-init-start.ps1
```
### 开发环境跑命令
```npm
npm run dev
```
