export interface SettingConfig {
    modelSize: number;
    focusMode: boolean;
    onTop: boolean;
    christmasEffect: boolean;
}

export interface LLMConfig {
    baseURL: string;
    apiKey: string;
}