/**
 * 商店
 */

type shopType = "normal" | "blackMarket"

export function showItems(type: shopType) {
    if (type === "normal") {
        // 正常商店
    } else if (type === "blackMarket") {
        // 黑市
    }
}